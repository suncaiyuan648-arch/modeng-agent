# Prisma boundary

WP-000 does not create predicted business tables. TALK Vertical Slice migrations will be added incrementally by the owning modules through `persistence-postgres` and the approved migration process.
